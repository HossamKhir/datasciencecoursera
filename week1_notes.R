#! /usr/bin/Rscript
x <- 1
# this is an expression
# '<-' is the assignment operator
# 'x' is a symbol
print(x)  # this is explicit print
x         # this is auto-print
# both give the output as [1] 1; where the '[1]' indicates where the first 
# element to its right lies in the vector being printed
msg <- "hello" # this is a character vector
